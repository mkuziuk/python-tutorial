---
title: "Классические модели: k-NN, логистическая регрессия, деревья, лес и SVM"
description: "Интуиция, сильные стороны и ограничения классических алгоритмов, используемых в делах Бюро."
concept: classical-models
usedIn: [part-2-case-01, part-2-case-02, part-2-case-03, part-2-case-04, part-2-case-05, part-2-case-06]
order: 36
---

В Части II модель — проверяемый компонент процедуры, а не персонаж с «интеллектом». Начинайте с baseline, собирайте preprocessing в pipeline и сравнивайте кандидатов на одинаковых разбиениях.

## k ближайших соседей (k-NN)

k-NN не строит явную формулу. Для нового объекта он находит `k` ближайших обучающих объектов и голосует их классами. Слова «вектор», «координата» и «расстояние» разобраны в [математическом словаре](../numpy/#math-vocabulary). Для двух векторов расстояние Евклида — длина разности:

```text
d(x, z) = √Σⱼ(xⱼ − zⱼ)²
```

Здесь `x` и `z` — два объекта с одинаковым набором признаков, индекс `j` перебирает признаки, а `Σ` означает «сложить». Для `x = [1, 2]` и `z = [4, 6]` разности равны `−3` и `−4`, поэтому расстояние равно `√(9 + 16) = 5`. Чем меньше число, тем ближе объекты в выбранном пространстве признаков.

```python
from sklearn.neighbors import KNeighborsClassifier
from sklearn.pipeline import make_pipeline
from sklearn.preprocessing import StandardScaler

knn = make_pipeline(
    StandardScaler(),
    KNeighborsClassifier(n_neighbors=5),
)
```

Масштабирование важно: признак с большими числами иначе доминирует в расстоянии. Например, разница в возрасте на 10 лет численно меньше разницы в стоимости билета на 100 единиц и почти исчезнет без общего масштаба. Малое `k` чувствительно к шуму, слишком большое сглаживает полезную локальную структуру.

## Логистическая регрессия

Несмотря на название, это классификатор. Он складывает признаки с весами, преобразует результат в вероятность класса и применяет порог.

До преобразования модель считает линейный score:

```text
score = b + w₁x₁ + w₂x₂ + …
```

`x₁, x₂` — значения признаков, `w₁, w₂` — коэффициенты, выученные по train, `b` — свободный член. Затем score плавно переводится в число от 0 до 1. Порог 0.5 — лишь возможное операционное правило, а не часть истины о классе.

```python
from sklearn.linear_model import LogisticRegression

classifier = LogisticRegression(max_iter=1000, random_state=42)
```

Модель дает сильный прозрачный baseline, быстро обучается и хорошо работает с one-hot признаками. Линейная граница может быть слишком простой, а величина коэффициента зависит от масштаба и кодирования. Положительный коэффициент означает рост score при прочих равных внутри модели; он не является доказательством причинного эффекта или важности в реальном мире.

## Дерево и случайный лес

Дерево последовательно задает вопросы вида `age <= 12.5`. Каждый вопрос делит текущую группу строк на две ветви; в листе остаётся прогноз для дошедших туда строк. Неглубокое дерево легко показать и объяснить, но глубокое может запомнить единичные train-примеры и переобучиться.

```python
from sklearn.tree import DecisionTreeClassifier

tree = DecisionTreeClassifier(
    max_depth=4,
    min_samples_leaf=10,
    random_state=42,
)
```

Случайный лес обучает много различающихся деревьев и объединяет их ответы: в классификации деревья голосуют, в регрессии их числовые прогнозы усредняются.

```python
from sklearn.ensemble import RandomForestRegressor

forest = RandomForestRegressor(
    n_estimators=200,
    min_samples_leaf=3,
    random_state=42,
    n_jobs=1,
)
```

Лес улавливает нелинейные связи и обычно не требует масштабирования. Он тяжелее, а стандартная feature importance может вводить в заблуждение и не является причинным объяснением.

## SVM с RBF-ядром

SVM ищет границу с большим зазором между классами. **Зазор** — свободная область между границей и ближайшими train-объектами разных классов. RBF-ядро позволяет изогнуть эту границу, сравнивая локальную близость объектов, хотя исходные признаки остаются прежними.

```python
from sklearn.svm import SVC

svm = SVC(C=10, gamma="scale", kernel="rbf")
```

- `C` управляет компромиссом между широким зазором и ошибками на train;
- `gamma` задает локальность влияния отдельного примера.

SVM чувствителен к масштабу признаков и может медленно работать на очень больших наборах. Для Digits он удобен, но это не делает его универсально лучшим алгоритмом.

## Как выбирать

Не выбирайте модель по train-score или одному удачному split. Зафиксируйте метрику, сравните кандидатов одной CV-процедурой, проверьте стабильность и ошибки, затем один раз откройте holdout. Чем сложнее модель и поиск, тем больше риск подгонки процедуры.

## Где встречается

k-NN вводится в [II-01](../../bureau/iris-exam/), логистическая регрессия и дерево сравниваются в [II-03](../../bureau/cost-of-one-error/), лес работает с ценами в [II-04](../../bureau/map-of-costly-errors/), а SVM — с цифрами в [II-05](../../bureau/familiar-handwriting/).
