---
title: "exceptions: контролируемые ошибки"
description: "Исключения в Python: как обработать плохие данные и продолжить расследование."
concept: "exceptions"
usedIn:
  - "case-03"
  - "case-04"
order: 10
---

## Что это

Исключение прерывает текущую операцию. Например, `ip_address()` вызывает `ValueError` для некорректного адреса. `except ValueError` обрабатывает этот случай, после чего программа может продолжить работу.

## Когда использовать

Используйте `try` / `except`, когда ошибка ожидаема и у программы есть понятное действие: пропустить запись, добавить предупреждение, показать аккуратный отчёт.

```python
try:
    number = int("17")
except ValueError:
    # Ноль допустим только как явно выбранный fallback для этой задачи.
    number = 0

print(number)
```

```text
17
```

Строка содержит допустимое целое, поэтому результат сохраняет исходное
значение. Fallback уместен лишь когда `0` не исказит смысл данных; иначе ошибку
нужно передать вызывающему коду.

## Мини-пример

Для отсутствующего обязательного файла безопаснее не подменять содержимое
пустой строкой, а сообщить, какой вход не найден:

```python
from pathlib import Path

try:
    text = Path("missing.txt").read_text(encoding="utf-8")
except FileNotFoundError as error:
    print(f"нет входного файла: {error.filename}")
```

```text
нет входного файла: missing.txt
```

Вывод фиксирует границу файловой системы и не создаёт выдуманные данные.
Когда есть несколько ожидаемых причин отказа, каждой можно дать своё
диагностическое сообщение.

## Ещё примеры

Ловите конкретную ожидаемую ошибку как можно ближе к операции, которая её вызывает:

```python
import json
from pathlib import Path

try:
    record = json.loads(Path("record.json").read_text(encoding="utf-8"))
except FileNotFoundError:
    print("файл record.json не найден")
except json.JSONDecodeError as error:
    # Позиция связывает сообщение с местом повреждения во внешнем JSON.
    print(f"JSON повреждён около символа {error.pos}")
else:
    print(f"загружено полей: {len(record)}")
```

`else` выполняется только если исключения не было. Это помогает не помещать дальнейшую обработку в слишком широкий блок `try`.
Для корректного `record.json` с объектом `{"id": 17}` результат будет
`загружено полей: 1`: `record` имеет форму `dict`, а число подтверждает только
успешный разбор верхнего уровня, не корректность значений полей.

## Типичные ловушки

- Не ловите голый `except:` без причины: он прячет даже неожиданные ошибки.
- Не продолжайте вычисление с выдуманным значением по умолчанию, если ошибка делает результат недостоверным.
- Не используйте исключения вместо обычной проверки, если проверка простая и ясная.
- В отчёте полезнее показать понятное предупреждение, чем полный traceback.

## Где встречается в расследованиях

- [Расследование 03. Фишинговое письмо или нет?](../../cases/phishing-email/) - записываем ошибку одного `.eml` и продолжаем обработку остальных файлов.
- [Расследование 04. Последнее доказательство](../../cases/final-evidence/) - останавливаем сборку отчёта, если вход отсутствует или имеет неверный ID.
