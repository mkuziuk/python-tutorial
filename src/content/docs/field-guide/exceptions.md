---
title: "exceptions: контролируемые ошибки"
description: "Исключения в Python: как обработать плохие данные и продолжить расследование."
concept: "exceptions"
usedIn:
  - "case-03"
  - "case-04"
order: 10
---

## Что это

Исключение - сигнал, что операция не смогла завершиться обычным способом. Например, строка оказалась не IP-адресом или JSON-файл поврежден.

## Когда использовать

Используйте `try` / `except`, когда ошибка ожидаема и у программы есть понятное действие: пропустить запись, добавить предупреждение, показать аккуратный отчет.

```python
try:
    number = int("17")
except ValueError:
    number = 0
```

## Мини-пример

```python
from pathlib import Path

try:
    text = Path("missing.txt").read_text(encoding="utf-8")
except FileNotFoundError:
    text = ""
```

## Типичные ловушки

- Не ловите голый `except:` без причины: он прячет даже неожиданные ошибки.
- Не используйте исключения вместо обычной проверки, если проверка простая и ясная.
- В отчете полезнее показать понятное предупреждение, чем полный traceback.

## Где встречается в делах

- [Дело 03. Фишинговое письмо или нет?](../../cases/phishing-email/) - проверяем URL и IP без падения на странных ссылках.
- [Дело 04. Ночной сигнал архива](../../cases/secret-folder-archive/) - аккуратно работаем с файлами и manifest JSON.
