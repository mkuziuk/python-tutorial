---
title: "Регрессия: MAE, RMSE, R² и остатки"
description: "Как оценивать числовой прогноз в исходных единицах и находить структуру ошибок, скрытую средней метрикой."
concept: regression
usedIn: [part-2-case-04]
order: 39
---

## Задача

Регрессия предсказывает число: цену, время, температуру. Прогноз `ŷᵢ` сравнивается с истинным `yᵢ`, а [остаток](../numpy/#math-vocabulary) обычно определяют так:

```text
rᵢ = yᵢ − ŷᵢ
```

Положительный остаток означает недооценку, отрицательный — переоценку, если используется именно это направление. Подпишите определение рядом с графиком.

Индекс `i` просто нумерует объекты. `n` ниже означает их количество, `Σᵢ` — «сложить по всем объектам», вертикальные черты `|…|` — взять абсолютную величину, а `√` — квадратный корень.

Небольшой пример в тысячах рублей: истинные значения `[200, 300, 500]`, прогнозы `[220, 260, 450]`. Остатки равны `[-20, 40, 50]`: первый объект переоценён на 20, два других недооценены.

## MAE

```text
MAE = Σᵢ |yᵢ − ŷᵢ| / n
```

MAE — средняя абсолютная ошибка в единицах цели. Если цель California Housing хранится в сотнях тысяч долларов, преобразуйте результат в доллары и явно назовите множитель.

Для примера выше `MAE = (20 + 40 + 50) / 3 ≈ 36.7` тысячи рублей. Знаки исчезли, поэтому переоценка и недооценка не компенсировали друг друга.

## RMSE

```text
RMSE = √(Σᵢ(yᵢ − ŷᵢ)² / n)
```

Возведение в квадрат сильнее наказывает крупные ошибки. Если RMSE заметно выше MAE, у модели есть редкие большие промахи или тяжелый хвост ошибок.

В том же примере `RMSE = √((20² + 40² + 50²) / 3) ≈ 38.7` тысячи рублей. Квадраты временно меняют единицу, а корень возвращает исходную.

## R²

```text
R² = 1 − Σᵢ(yᵢ − ŷᵢ)² / Σᵢ(yᵢ − ȳ)²
```

`ȳ` читается как «игрек с чертой» и означает среднее истинных значений **в оцениваемой выборке**. Знаменатель — ошибка гипотетического ориентира, который для всех этих объектов предсказывает это среднее. `R² = 1` соответствует идеальным прогнозам; около нуля — этому ориентиру на той же выборке; значение может быть отрицательным. Например, `R² = 0.8` означает, что сумма квадратов ошибок модели составляет 20% от ошибки такого ориентира, а не что «80% прогнозов верны».

Это определение метрики, а не инструкция обучать базовую модель на тестовой выборке. Настоящий `DummyRegressor` вычисляет среднее только по обучающей выборке; при сдвиге между обучением и тестом его `R²` может отличаться от нуля и даже быть отрицательным.

```python
from sklearn.metrics import mean_absolute_error, r2_score, root_mean_squared_error

y_test = [200, 300, 500]
predictions = [220, 260, 450]

mae = mean_absolute_error(y_test, predictions)
rmse = root_mean_squared_error(y_test, predictions)
r2 = r2_score(y_test, predictions)
print(f"MAE={mae:.1f}; RMSE={rmse:.1f}; R²={r2:.3f}")
```

```text
MAE=36.7; RMSE=38.7; R²=0.904
```

MAE и RMSE здесь измеряются в тысячах рублей, меньше — лучше; `R²` безразмерен,
и больше — лучше. RMSE выше MAE на 2 тысячи рублей, потому что сильнее
взвешивает крупные ошибки. `R²=0.904` означает, что сумма квадратов ошибок
составляет около 9,6% от знаменателя со средним оцениваемой выборки; это не
доля верных прогнозов. Мы получили сводные числа, но они не показывают
направление отдельных промахов, поэтому следующим этапом служит анализ
остатков.

## Остатки

Средняя метрика не показывает, где модель ошибается. Проверьте:

- остаток против предсказания;
- абсолютную ошибку по диапазонам цели;
- карту, время или партию, если они относятся к условиям применения;
- ошибки у искусственной верхней или нижней границы целевой переменной.

Идеальная «случайная полоса вокруг нуля» не является строгим тестом, но явный веер, изгиб или географический кластер указывает на систематическую структуру.

Автономная проверка направления для тех же трёх объектов:

```python
import numpy as np

residuals = np.asarray(y_test) - np.asarray(predictions)
print(residuals)
```

```text
[-20  40  50]
```

При принятой границе `остаток = факт − прогноз` значение `-20` означает
переоценку на 20 тысяч рублей, а `40` и `50` — недооценку. Если на графике
поменять порядок вычитания, смысл знака тоже поменяется; подпись оси должна
это отражать.

## Базовая модель и разбиение

Сравните модели с `DummyRegressor`, предсказывающим среднее или медиану обучающей выборки. Оценивайте все варианты на одном разбиении. Если задача обещает новый регион, случайная тестовая выборка из соседних кварталов не проверяет это заявление.

## Типичные ловушки

- Называть `R² = 0.8` «80% правильных прогнозов».
- Путать единицы целевой переменной и отчёта.
- Считать только среднее и не видеть сегмент крупных ошибок.
- Удалять значения на искусственной границе как «выбросы» без документации.
- Сравнивать MAE моделей на разных тестовых наборах.

## Где встречается

Все понятия применяются в [II-04 · Карта дорогих ошибок](../../bureau/map-of-costly-errors/).
