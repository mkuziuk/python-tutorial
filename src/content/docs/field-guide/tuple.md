---
title: "tuple: неизменяемая связка"
description: "Кортежи в Python: как хранить короткую группу значений и использовать ее как ключ."
concept: "tuple"
usedIn:
  - "case-02"
order: 7
---

## Что это

`tuple` - неизменяемая последовательность. В расследованиях кортеж удобен, когда несколько значений вместе образуют один признак.

## Когда использовать

Используйте кортеж, если порядок важен, а менять элементы не нужно. Например, n-грамма из трех слов может быть кортежем.

```python
trigram = ("ночной", "архив", "молчит")
```

## Мини-пример

```python
words = ["ночной", "архив", "молчит", "снова"]
ngrams = [tuple(words[index:index + 3]) for index in range(len(words) - 2)]
print(ngrams)
```

## Типичные ловушки

- Кортеж из одного элемента пишется с запятой: `("след",)`.
- Кортеж нельзя изменить на месте. Если нужен новый вариант, создайте новый кортеж.
- Список нельзя положить в `set`, а кортеж из строк можно.

## Где встречается в делах

- [Дело 02. Детектор текстовых совпадений](../../cases/copy-paste-detector/) - n-граммы как кортежи слов.
