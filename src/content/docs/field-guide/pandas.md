---
title: "pandas: таблицы, типы и пропуски"
description: "Быстрый аудит DataFrame: форма, dtypes, пропуски, категории, выбор столбцов и групповые сводки."
concept: pandas
usedIn: [part-2-case-01, part-2-case-02, part-2-case-03, part-2-case-04, part-2-case-06]
order: 32
---

## Что это

`DataFrame` — таблица со строками-наблюдениями, именованными столбцами и индексом. `Series` — один столбец. Имена помогают аудировать признаки до того, как таблица превратится в матрицу модели.

Если слова «матрица», «вектор» или `shape` пока непривычны, сначала откройте [математический словарь Части II](../numpy/#math-vocabulary).

Например, одна строка может описывать пассажира, а столбцы — известные о нём поля:

```python
import pandas as pd

df = pd.DataFrame({
    "age": [22.0, None, 35.0],
    "sex": ["female", "male", "female"],
    "survived": [1, 0, 1],
})

print(df.shape)           # (3, 3): три строки, три столбца
print(df["age"].isna())  # False, True, False
```

Индекс `0, 1, 2` помогает pandas выравнивать строки, но сам по себе не обязан быть признаком модели. `None` в числовом столбце превращается в пропуск, а не в возраст 0.

Для готового снимка данных обычно достаточно:

```python
df = pd.read_csv("data/titanic.csv")
print(df.shape)
df.head()
```

## Первый аудит

Не начинайте с обучения. Сначала ответьте на четыре вопроса:

```python
df.info()
df.dtypes
df.isna().mean().sort_values(ascending=False)
df.nunique(dropna=False).sort_values()
```

- Сколько строк и столбцов?
- Какая строка считается независимым наблюдением?
- Какие столбцы числовые, категориальные или идентификаторы?
- Где пропуски и что они означают?

Тип `object` не гарантирует, что столбец — полезная категория. Это может быть имя, свободный текст, смешанный формат или число, прочитанное как строка.

## Признаки и цель

```python
target = "survived"
features = ["pclass", "sex", "age", "fare", "embarked"]

X = df[features].copy()
y = df[target].copy()
```

Явный список признаков безопаснее, чем «все столбцы, кроме цели»: так идентификаторы и post-outcome поля не попадут в модель случайно.

## Пропуски

Пропуск — не ноль и не пустая строка. Его обработка зависит от смысла:

```python
df["age"].isna().sum()
df.groupby("pclass", dropna=False)["age"].median()
```

В Части II пропуски заполняет `SimpleImputer` внутри `Pipeline`. Если вычислить медиану по всей таблице до разбиения, информация из тестовой выборки повлияет на подготовку данных.

## Групповые сводки

```python
summary = (
    df.groupby("pclass", dropna=False)
      .agg(rows=("survived", "size"), survival_rate=("survived", "mean"))
      .reset_index()
)
```

Если `survived` кодируется как 0/1, его среднее равно доле единиц: для `[1, 0, 1, 1]` это `3 / 4 = 0.75`. Всегда показывайте `rows` рядом с долей. Показатель 100% для группы из двух строк намного менее устойчив, чем 80% для группы из сотен.

## Типичные ловушки

- Интерпретировать индекс как признак.
- Терять соответствие `X` и `y` после раздельной сортировки или удаления строк.
- Делать chained assignment и не понимать, изменилась ли исходная таблица.
- Заменять все пропуски одним значением до разбиения.
- Считать корреляцию доказательством доступности или причинности.

## Где встречается

[II-02](../../bureau/passengers-after-the-fact/) строится вокруг аудита столбцов Titanic. [II-03](../../bureau/cost-of-one-error/) использует `groupby()` для срезов ошибок, а [II-06](../../bureau/compass-exam/) — для проверки пересечения источников.
