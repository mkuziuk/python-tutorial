# Ожидаемая форма результата II-04

## Данные
- SHA-256 совпадает с data/dataset_manifest.json.
- 20 640 строк, 8 признаков, целевая переменная `MedHouseVal`, пропусков нет.
- Не менее 900 строк находятся на задокументированной верхней границе 5.00001.

## Разбиения
- Случайное разбиение: 16 512 обучающих / 4 128 тестовых строк.
- Географические группы: ячейки 0,5° × 0,5°.
- Пересечение `region_id` между групповой обучающей и тестовой выборками равно 0.

## Качественные проверки
- Случайный лес превосходит `DummyRegressor` при случайном разбиении.
- Его RMSE при случайном разбиении — примерно 0,50 (около USD 50 000).
- Его RMSE на невиданных регионах — примерно 0,66 (около USD 66 000) и как минимум
  на 0,08 единицы хуже случайного.
- Показаны остатки, карта абсолютной ошибки, ценовые и north/south срезы с n.

## Артефакт
- artifacts/audit_memo.md содержит пять разделов и не объявляет корреляцию
  причинностью или доказательством умысла.
