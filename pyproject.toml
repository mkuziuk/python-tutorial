[project]
name = "investigative-python-projects"
version = "0.1.0"
description = "Project files for the Russian investigative Python tutorial."
requires-python = ">=3.14"
dependencies = [
  "rich==15.0.0",
]
